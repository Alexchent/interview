<?php/** *1. $array1+$array22. array_merge_recursive()2. array_merge- 对应相同的字符串索引，“+” 前面索引的值会覆盖后面索引的值array_merge 后面的值覆盖前面的值array_merge_recusive 会将相同的索引放到一个数组中，增加数组纬度- 对于相同的数字索引“+” 还是前面的覆盖后面的array_merge 则会给重复的值重建索引。array_merge_recusive跟"+"一样 * */$a = [1,2];$b = [2,3];//var_dump($a+$b);//var_dump(array_merge($a, $b));//var_dump(array_merge_recursive($a+$b));$str1 = ['a'=> 1, 'b'=>2];$str2 = ['a'=> 2, 'b'=>3];var_dump($str1+$str2);var_dump(array_merge($str1, $str2));var_dump(array_merge_recursive($str1, $str2));