<?php/** * Created by PhpStorm. * User: chentao * Date: 2021/7/3 * Time: 10:54 AM *///注册自动加载spl_autoload_register('autoload');function autoload($class){    require dirname($_SERVER['SCRIPT_FILENAME'])  .'/' .str_replace('\\', '/', $class) . '.php';}//适配器模式//当个了两个接口不匹配时，需要通过第三方接口进行转换，比如给手机充电的数据线无法直接插到家庭电源插座中时，需要一个适配器来做电压转换//通常适配器模式是是在项目迭代过程中出现的，设计值出两个接口应当可以直接匹配$chineseHomeBattery = new ChineseHomeBattery();//中国家庭电源$adapter = new IPhoneAdapter(); //iPhone充电器$usbline = new IPhoneUseLine(); //iphone数据线$usbline->charge($adapter->convert($chineseHomeBattery));